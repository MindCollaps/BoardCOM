# Management App (placeholder)

Native Android (Kotlin) management app for configuration/setup while parked,
connecting to the ESP32 gateway over BLE. Not started yet — the current build
focus is the gateway firmware (see `/firmware`).
